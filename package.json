{
  "name": "lifeos-root",
  "version": "1.0.0",
  "private": true,
  "description": "LifeOS — AI-Powered Deadline & Life Management System",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node scripts/run-with-env.js node --max-old-space-size=256 backend/dist/server.js",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && node scripts/copy-build.js",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:seed": "npm run seed --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
